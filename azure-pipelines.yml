# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- main

pool: 
  name: Default # Asegurarse de usar el nombre del pool que se configuro en azure devops.

jobs:
- job: BuildOnLinux
  displayName: 'Build on Linux'
  pool:
    name: Default
    demands:
      - os -equals linux
  steps:
  - task: UsePythonVersion@0
    inputs:
      versionSpec: '3.11'
    displayName: 'Setup Python 3.x on Linux'
    
  - script: |
      python -m pip install --upgrade pip
      pip install -r requerimientos.txt
    displayName: 'Install dependencies'

  - script: |
      python -m unittest
    displayName: 'Run tests on Linux'

- job: BuildOnWindows
  displayName: 'Build on Windows'
  pool:
    name: Default
    demands:
      - os -equals windows
  steps:
  - task: UsePythonVersion@0
    inputs:
<<<<<<< HEAD
      versionSpec: '3.x'
      addToPath: true
      architecture: 'x64'
    condicion: and(succeded(), eq(variables['Agent.OS'], 'windows'))
=======
      versionSpec: '3.11'
      addToPath: true
      architecture: 'x64'
    condition: and(succeeded(), eq(variables['Agent.OS'], 'windows'))
>>>>>>> d0fe495a71658a4ca9594a4bdefc9b37c247d022
    displayName: 'Setup Python 3.x on Windows'

  - script: |
      python -m pip install --upgrade pip
<<<<<<< HEAD
      pip install -r requirimentos.txt
=======
      pip install -r requerimientos.txt
>>>>>>> d0fe495a71658a4ca9594a4bdefc9b37c247d022
    displayName: 'Install dependencies'

  - script: |
      python -m unittest
    displayName: 'Run tests on Windows'

